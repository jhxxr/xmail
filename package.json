{
  "name": "xmail",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "corepack pnpm --filter web dev",
    "build": "corepack pnpm --filter web build",
    "deploy": "corepack pnpm --filter web run deploy && corepack pnpm --filter email-worker run deploy",
    "db:generate": "corepack pnpm --filter database db:generate",
    "db:migrate": "corepack pnpm --filter database db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
